import Register from '../register/Register';
import styles from './styles.module.scss';

const ContentRegister = () => {
	return (
		<section className={styles.contentRegister}>
			<Register />
		</section>
	);
};
export default ContentRegister;
